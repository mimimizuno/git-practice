name: Change shell
on: push
jobs:
  print:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - shell: bash
        run: echo "{SHELL}"
      - shell: python
        run: |
          import os
          print(os.environ['PATH'])
